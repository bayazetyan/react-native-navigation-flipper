{
  "name": "react-native-navigation-flipper",
  "version": "0.1.0",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "license": "MIT",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "yarn run build",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest"
  },
  "peerDependencies": {
    "react-native": ">=0.63.0",
    "react-native-flipper": ">=0.100.0",
    "react-native-navigation": ">=7.17.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "flipper-plugin": "^0.176.1",
    "prettier": "^2.8.1",
    "react-native-flipper": "^0.176.1",
    "typescript": "^4.9.4"
  }
}
